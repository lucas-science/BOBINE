{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Allow frontend to use the dialog plugin (open/save).",
  "windows": ["main"],    
  "webviews": ["main"], 
  "permissions": ["dialog:default"]
}
